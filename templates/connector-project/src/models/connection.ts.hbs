export interface ConnectionConfig {
  connectionId: string;
  connectionName: string;
  connectionDescription: string;
  aclMode: 'everyone' | 'everyoneExceptGuests' | 'none';
}

export const connection: ConnectionConfig = {
  connectionId: '{{connectorId}}',
  connectionName: '{{connectorName}}',
  connectionDescription: '{{connectorDescription}}',
  aclMode: '{{aclMode}}',
};

// ---------------------------------------------------------------------------
// urlToItemResolver — link unfurling in Teams / Copilot
//
// When a user shares a link in Teams or Copilot that matches the pattern,
// the connector shows an inline preview / Adaptive Card.
//
// Reference:
//   https://learn.microsoft.com/graph/connecting-external-content-manage-connections#urltoitemresolver
// ---------------------------------------------------------------------------
{{urlToItemResolverBlock}}

